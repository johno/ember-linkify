import {
  linkify
} from 'ember-linkify/helpers/linkify';

module('LinkifyHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = linkify(42);
  ok(result);
});
